<Window x:Class="GUI.Windows.Views.DarkDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" SizeToContent="Height"
        Width="420" MinHeight="160"
        Background="#1E1E1E" Foreground="White"
        WindowStyle="None" AllowsTransparency="True"
        BorderBrush="#3E3E42" BorderThickness="1">

    <Grid Margin="24,20,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titolo -->
        <TextBlock Grid.Row="0" x:Name="TitleText" FontSize="15" FontWeight="SemiBold" Foreground="White"
                   TextWrapping="Wrap" Margin="0,0,0,12"/>

        <!-- Messaggio -->
        <TextBlock Grid.Row="1" x:Name="MessageText" FontSize="13" Foreground="#CCCCCC"
                   TextWrapping="Wrap" Margin="0,0,0,20"/>

        <!-- Bottoni -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SecondaryButton" Content="No" MinWidth="80" Margin="0,0,8,0"
                    Style="{StaticResource ToolbarButtonStyle}" Click="SecondaryButton_Click"
                    Visibility="Collapsed"/>
            <Button x:Name="PrimaryButton" Content="OK" MinWidth="80"
                    Style="{StaticResource ToolbarButtonStyle}" Background="#004682" FontWeight="SemiBold"
                    Click="PrimaryButton_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>

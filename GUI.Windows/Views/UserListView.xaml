<UserControl x:Class="GUI.Windows.Views.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:GUI.Windows.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:UserListViewModel}"
             Loaded="UserControl_Loaded">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Utenti" 
                       FontSize="24" 
                       FontWeight="SemiBold"/>
            <TextBlock Text="Gestione degli utenti che possono modificare i dizionari." 
                       Foreground="Gray" 
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Form nuovo utente (inline) -->
        <Border Grid.Row="1" 
                Background="#F5F5F5" 
                BorderBrush="#CCCCCC" 
                BorderThickness="1" 
                Padding="16" 
                Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Nuovo Utente" FontWeight="SemiBold" Margin="0,0,0,12"/>
                <StackPanel Orientation="Horizontal">
                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="Username *" FontSize="11" Foreground="Gray"/>
                        <TextBox Text="{Binding NewUsername, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="150" Padding="8,6" Margin="0,4,0,0"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="Nome Visualizzato *" FontSize="11" Foreground="Gray"/>
                        <TextBox Text="{Binding NewDisplayName, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="200" Padding="8,6" Margin="0,4,0,0"/>
                    </StackPanel>
                    <Button Command="{Binding AddCommand}" 
                            Content="+ Aggiungi" 
                            Padding="16,8"
                            VerticalAlignment="Bottom"
                            Background="#0078D4" 
                            Foreground="White"
                            FontWeight="SemiBold"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Lista utenti -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Users}"
                  SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  BorderThickness="1"
                  BorderBrush="#CCCCCC">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" 
                                    Binding="{Binding Id}" 
                                    Width="60"/>
                <DataGridTextColumn Header="Username" 
                                    Binding="{Binding Username}" 
                                    Width="200"/>
                <DataGridTextColumn Header="Nome Visualizzato" 
                                    Binding="{Binding DisplayName}" 
                                    Width="*"/>
                
                <!-- Colonna azioni -->
                <DataGridTemplateColumn Header="Azioni" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="🗑️" 
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Padding="6,2"
                                        ToolTip="Elimina"
                                        Foreground="Red"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Error message -->
        <Border Grid.Row="3" 
                Background="#FFEEEE" 
                BorderBrush="#FF6666" 
                BorderThickness="1" 
                Padding="8" 
                Margin="0,8,0,0"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#CC0000" TextWrapping="Wrap"/>
        </Border>

        <!-- Loading overlay -->
        <Border Grid.Row="0" Grid.RowSpan="4" 
                Background="#80FFFFFF" 
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20"/>
                <TextBlock Text="Caricamento..." Margin="0,8,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

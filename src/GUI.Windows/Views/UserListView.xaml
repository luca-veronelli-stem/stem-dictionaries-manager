<UserControl x:Class="GUI.Windows.Views.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:GUI.Windows.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:UserListViewModel}"
             Loaded="UserControl_Loaded"
             Background="#1E1E1E">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Form nuovo utente -->
        <Border Grid.Row="0" Background="#252526" BorderBrush="#3E3E42" BorderThickness="1" 
                CornerRadius="6" Padding="16" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Nuovo Utente" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,12"/>
                <StackPanel Orientation="Horizontal">
                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="Username *" FontSize="11" Foreground="#888888"/>
                        <TextBox Text="{Binding NewUsername, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource WatermarkTextBoxStyle}" Width="150" Margin="0,4,0,0"
                                 Tag="username"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="Nome Visualizzato *" FontSize="11" Foreground="#888888"/>
                        <TextBox Text="{Binding NewDisplayName, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource WatermarkTextBoxStyle}" Width="200" Margin="0,4,0,0"
                                 Tag="nome cognome"/>
                    </StackPanel>
                    <Button Command="{Binding AddCommand}" Content="➕ Aggiungi" 
                            Style="{StaticResource ToolbarButtonStyle}" VerticalAlignment="Bottom"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="🔄 Aggiorna" Command="{Binding RefreshCommand}" Style="{StaticResource ToolbarButtonStyle}"/>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Style="{StaticResource SearchTextBoxStyle}" Margin="8,0,0,0"/>
        </StackPanel>

        <!-- Errore -->
        <Border Grid.Row="2" Background="#E40032" Padding="10" Margin="0,0,0,10" CornerRadius="4"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="White" TextWrapping="Wrap"/>
        </Border>

        <!-- Lista utenti -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedItem}"
                  Style="{StaticResource DarkDataGridStyle}"
                  ColumnHeaderStyle="{StaticResource DarkDataGridColumnHeaderStyle}"
                  RowStyle="{StaticResource DarkDataGridRowStyle}"
                  CellStyle="{StaticResource DarkDataGridCellStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" MinWidth="40"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="200" MinWidth="100"/>
                <DataGridTextColumn Header="Nome Visualizzato" Binding="{Binding DisplayName}" Width="*" MinWidth="120"/>
                <DataGridTemplateColumn Header="Azioni" Width="80" MinWidth="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="🗑️" Style="{StaticResource ToolbarButtonStyle}" Padding="8,4"
                                    Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Loading -->
        <Border Grid.Row="0" Grid.RowSpan="4" Background="#80000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressBar IsIndeterminate="True" Width="200" Height="4" VerticalAlignment="Top"/>
        </Border>
    </Grid>
</UserControl>
